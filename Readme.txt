1. Run Create.sql script under SiteCoreSolution/Database in sql server
2. Change ConnectionString in webconfig under SiteCoreSolution
3. Finally browse the SiteCoreSolution/SiteCoreWebService.asmx to initiate instance and run the SiteCoreWebSite solution
